spring:
  application:
    name: api-gateway-service
  cloud:
    gateway:
      routes:
        # Auth Service Route
        - id: auth-service
          uri: http://dev-auth-service
          predicates:
            - Path=/api/v1/auth/**
          filters:
            - RewritePath=/api/v1/auth(?<segment>/?.*), /api/v1/auth$\{segment}

        # Course Command Service Route (Write operations)
        - id: course-command-service
          uri: http://dev-course-command-service
          predicates:
            - Path=/api/v1/courses/**
            - Method=POST,PUT,DELETE
          filters:
            - AuthenticationFilter
            - RewritePath=/api/v1/courses(?<segment>/?.*), /api/v1/courses$\{segment}

        # Course Query Service Route (Read operations)
        - id: course-query-service
          uri: http://dev-course-query-service
          predicates:
            - Path=/api/v1/courses-query/**
            - Method=GET
          filters:
            - AuthenticationFilter
            - RewritePath=/api/v1/courses-query(?<segment>/?.*), /api/v1/courses-query$\{segment}

        # Enrollment Service Route
        - id: enrollment-service
          uri: http://dev-enrollment-service
          predicates:
            - Path=/api/v1/enrollments/**
          filters:
            - AuthenticationFilter
            - RewritePath=/api/v1/enrollments(?<segment>/?.*), /api/v1/enrollments$\{segment}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

jwt:
  secret-key: ${JWT_SECRET:NDA0RTYzNTI2NjU1NkE1ODZFMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNjc1NjZCNTk3MA==}

logging:
  level:
    root: INFO
    com.bi: DEBUG
    org.springframework.cloud.gateway: DEBUG
